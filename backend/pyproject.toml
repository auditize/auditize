[project]
name = "Auditize"
version="0.1.0-dev"
description = "Audit logs management system"
dependencies=[
    "fastapi",
    "pydantic[email]",
    "python-multipart",
    "uvicorn[standard]",
    "motor",
    "aiocache",
    "certifi",
    "bcrypt",
    "Authlib",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "pytest-xdist",
    "callee",
    "httpx",
    "icecream",
    "ruff",
]

[tool.ruff]
src = ["src", "tests", "scripts"]
fix = true

[tool.ruff.lint]
select = [
    "I",  # isort
]
